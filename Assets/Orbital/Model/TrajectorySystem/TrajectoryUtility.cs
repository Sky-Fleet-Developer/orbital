using UnityEngine;

namespace Orbital.Model.TrajectorySystem
{
    public static class TrajectoryUtility
    {
        /*public static Matrix4x4 GetMatrix(this RelativeTrajectory trajectory)
        {
            double ratio = trajectory.SemiMajorAxis / trajectory.SemiMinorAxis;
            return Matrix4x4.TRS();
        }*/
    }
}
